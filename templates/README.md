# 通用模板目录

此目录只保存可以公开复用的结构和字段约定。真实招标文件中的投标函、授权书、报价表、偏离表等，应原样导入对应投标项目，并在项目的 `forms` 主记录中登记和核准。

表单占位符使用 `{{field_name}}`。程序在保持 DOCX 现有段落、表格和页眉页脚格式的前提下替换占位符；字段来自项目 `facts` 或该表单的 `fields`。常用字段见 `表单字段示例.json`。

填充成功只表示字段已写入。模板来源、格式、必填项、报价以及签字盖章仍需按本项目招标原文核对。
